{
  "name": "plate-pulse-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend package manifest for the Plate Pulse FastAPI app.",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
