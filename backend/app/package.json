{
  "name": "backend-app",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal package.json to avoid npm ENOENT when running npm in backend/app. This project is Python-based; use Python to run the backend.",
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  }
}
